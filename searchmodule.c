#include <stdio.h>
#include <stdlib.h>

int max(void) {
  // allocate ints for readin
  int num1;
  int num2;
  // read in input
  scanf("X: %d", num1);
  scanf("Y: %d", num2);
  // initialize result int
  int result;
  // compare and return
  if (num1 > num2)
    result = num1;
  else
    result = num2;
  return result;
}

int mario(int height) {
  for 
}

char last(char c1, char c2) {
  char result;

  int a1 = c1;
  int a2 = c2;

  if (a1 > a2)
    result = c1;
  else
    result = c2;
  return result;
}

int main() {
  int mario();
}
